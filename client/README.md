# Client README

This file is only for the frontend app.

For full project setup, running, submission instructions, and production commands, use the root README:

- `../README.md`

Client commands:

- `npm start` - run frontend in development
- `npm run build` - build frontend for production
- `npm test` - run frontend tests

This project was originally created with Create React App, but the root README is the real source of truth for this repository.
